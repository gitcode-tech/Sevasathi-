const express = require("express");
const router = express.Router();
const Land = require("../models/Land");

router.get("/:survey",async(req,res)=>{
 const land = await Land.findOne({surveyNumber:req.params.survey});
 res.json(land);
});

module.exports = router;
